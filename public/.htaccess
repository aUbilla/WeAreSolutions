Options -MultiViews
RewriteEngine On

# Los archivos .php se sirven directamente — no pasan por el SPA
RewriteRule ^api/contact\.php$ - [L]

# Todo lo demás que no sea un archivo real se redirige a React
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]
